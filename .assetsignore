# Files that must not be published as part of the site.
# Pages excluded these automatically; Workers does not.
.git
.gitignore
.assetsignore
wrangler.jsonc
README.md

# Wrangler writes temp build artifacts here during `wrangler deploy`.
# Without this they get published and served as part of the site.
.wrangler
node_modules
